name: Call the Jira workflow
on:
  workflow_call:
    branches:
      - 'main'
jobs:
  call-workflow:
    runs-on: ubuntu-latest
    steps:
      - uses: sasidharankp/hooks/.github/workflows/jira.yml@v24.4.0

      - name: Trigger Jira workflow  
        run: echo "Workflow Triggered"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
